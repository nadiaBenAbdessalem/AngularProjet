import { CustomerdirecDirective } from './customerdirec.directive';

describe('CustomerdirecDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomerdirecDirective();
    expect(directive).toBeTruthy();
  });
});
